Index: libibm-1.0.0/Makefile
===================================================================
--- libibm-1.0.0.orig/Makefile
+++ libibm-1.0.0/Makefile
@@ -322,8 +322,8 @@ paho-mqtt-download:
 	fi
 
 paho-mqtt-build:
-	echo "Build paho mqtt c"
-	make -C paho.mqtt.c
+	echo "Build paho mqtt c."
+	make -C paho.mqtt.c GAI_LIB=
 
 tests:
 	make -C test
